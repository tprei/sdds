name: sdds

services:
  api:
    build:
      context: ../..
      dockerfile: services/api/Dockerfile
    environment:
      SDDS_HTTP_ADDR: :8080
      SDDS_DATABASE_PATH: /data/sdds.db
    ports:
      - "8080:8080"
    volumes:
      - api-data:/data

volumes:
  api-data:
